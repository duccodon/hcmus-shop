# Project Status — 2026-04-13 (Week 1, Mid-week)

Snapshot of progress vs `docs/plans/project-roadmap-3-weeks.md`.

---

## Quick legend

- ✅ Done — works end-to-end
- 🟡 Partial — started, not finished
- ❌ Not started

---

## Overall progress

| Area | Status |
|------|--------|
| Foundation (auth, GraphQL, DI, MVVM, login) | ✅ Done |
| Products CRUD | 🟡 Partial — list + filter wired, AddProduct not done |
| Backend feature modules (Order, Customer, Promotion, Report) | ❌ Not started |
| Dashboard | 🟡 UI exists with mock data |
| Bonus features | ❌ All not started |

---

## Dev A (Dung) — my tasks

### WEEK 1 — Foundation polish

**Backend**
- ✅ Express + Apollo + Prisma server set up
- ✅ Auth feature (login, me query, JWT plugin)
- ✅ Brand / Category / Series / Product resolvers
- ✅ Docker Compose for PostgreSQL
- ✅ `.env.example` file
- 🟡 Realistic seed data — currently ~16 products, target is ~25 per category (need to expand)
- ❌ **Image upload endpoint `POST /uploads`** with multer
- ❌ **Static file serving** `/uploads`
- ❌ **`dashboardStats` resolver** (totalProducts, ordersToday, revenueToday, lowStock, topSelling, recentOrders, dailyRevenue)
- ❌ **Role-based access**: `Product.importPrice` should return null for Sale role

**Client**
- ✅ GraphQL client wrapper with JWT
- ✅ DI container, services, DTOs
- ✅ Login page redesigned, server URL displayed
- ✅ AuthService with token persistence (LocalSettings)
- ❌ **`ConfigPage.xaml`** (Server URL input + Save button)
- ❌ **Auto-login flow** using `TryAutoLoginAsync` on app startup
- 🟡 `DashboardPage.xaml` exists but uses mock data — need to wire to real GraphQL
- ❌ **`SettingsPage.xaml`** (page size, last screen toggle)
- ❌ **Hide import price column** for Sale role on Products page

### WEEK 2 — Dashboard + Settings + Responsive

- ❌ `IDashboardService` + DashboardDto
- ❌ Wire DashboardPage to real `dashboardStats` query
- ❌ KPI cards with real numbers
- ❌ Low stock table, recent orders table, revenue chart
- ❌ SettingsPage with page size dropdown
- ❌ Track and restore last-opened screen
- ❌ Responsive layout pattern + own pages (Login, Dashboard, Settings)

### WEEK 3 — Installer + Bonus features

- ❌ Backup endpoint `POST /backup` (pg_dump)
- ❌ Restore endpoint `POST /restore`
- ❌ Backup/Restore UI in Settings
- ❌ Trial mode (15-day lock) with activation code `HCMUS2026`
- ❌ Onboarding tutorial (TeachingTip on first login)
- ❌ Configure `Package.appxmanifest` (app name, logo, description)
- ❌ Obfuscator post-build step (ConfuserEx or Obfuscar)
- ❌ Build MSIX installer
- ❌ Unit tests (~3-4 tests for auth + role-based filtering)

---

## Dev B (Buu) — Products & Promotions

### WEEK 1 — Products feature

**Backend**
- ✅ Product / Brand / Category / Series resolvers (built in shared foundation)

**Client**
- ✅ Products list with server pagination
- ✅ Brand + Category filters wired to GraphQL
- 🟡 ProductsViewModel — sort UI may be incomplete
- 🟡 AddProductPage — exists but image upload not wired (waits on Dev A's `/uploads` endpoint)
- ❌ Search by name with debounce
- ❌ Edit existing product (ProductDetailPage)
- ❌ Delete with confirmation dialog

### WEEK 2

- ❌ Promotion feature module (backend)
- ❌ `PromotionsPage.xaml`
- ❌ Promotion code input in CreateOrderPage (waits on Dev C)
- ❌ Multi-criteria sort UI (chip-style with direction)
- ❌ Advanced search (multi-condition: name + SKU + price + category + brand)
- ❌ Auto-save on AddProduct form

### WEEK 3

- ❌ Tier promotions (`minLoyaltyPoints` field)
- ❌ Polish + bug fixes

---

## Dev C (Duc) — Orders, Reports, Customers

### WEEK 1 — Orders scaffold

**Backend**
- ❌ Customer feature module (CRUD + loyaltyPoints)
- ❌ Order feature module (resolver, service, repository, dto)
- ❌ `orders`, `order`, `customers` queries
- ❌ `createOrder`, `updateOrderStatus`, `deleteOrder` mutations
- ❌ Inventory sync on Paid (instances → Sold, stock decrement, InventoryLog, loyalty points)

**Client**
- ❌ `IOrderService`, `OrderService`, OrderDtos
- ❌ `OrdersPage.xaml` (list, pagination, date filter, status badges)
- ❌ `OrderDetailPage.xaml` (items, status change buttons)

### WEEK 2 — Order creation flow

- ❌ `CreateOrderPage.xaml` (3 steps: customer / items / review)
- ❌ Running total, promo code input, confirm button
- ❌ Auto-save order draft

### WEEK 3 — Reports + Customers + PDF

- ❌ Reports resolvers (salesReport, topProducts, salesByCategory)
- ❌ `ReportPage.xaml` with 3 charts
- ❌ `CustomersPage.xaml`
- ❌ Customer detail with loyalty/order history
- ❌ Print order to PDF (QuestPDF)
- ❌ Sale KPI widget on Dashboard
- ❌ Customer stats query

---

## Today's action plan (Dev A focus — finish all Week 1 tasks)

Priority order (do these in sequence):

### Backend (estimated ~3 hours)
1. **Add image upload endpoint** — install multer, add `POST /uploads` route, save files to `uploads/products/`, return URL
2. **Add static file serving** — `app.use('/uploads', express.static('uploads'))`
3. **Implement role-based access** — modify `Product.importPrice` resolver to return `null` for non-Admin
4. **Expand seed data** — bump to 25 products per category (5 categories = 125 products)
5. **Add `dashboardStats` resolver** — single query returning all KPIs

### Client (estimated ~3 hours)
6. **Create `ConfigPage.xaml`** — server URL input, Save button, navigation from LoginPage
7. **Implement auto-login** — call `TryAutoLoginAsync` on app startup, skip login screen if successful
8. **Create `SettingsPage.xaml`** — page size dropdown, last screen toggle
9. **Create `IDashboardService`** + DashboardDto + wire DashboardPage to real data
10. **Hide import price column** for Sale role on Products page

### Verification (today)
- [ ] Server starts, `/uploads` accepts a file
- [ ] Database has 125 products
- [ ] ConfigPage opens from login, saves URL
- [ ] Close app + reopen → auto-login works (with Remember me)
- [ ] Sale account: import price column hidden
- [ ] Dashboard shows real KPIs
- [ ] Settings page size affects Products page

---

## Blockers / dependencies

- **Dev B's AddProduct image upload** waits on my `/uploads` endpoint — high priority to unblock Dev B
- **Dev C's order inventory log** doesn't depend on me, but Dashboard's `recentOrders` field will need real orders eventually (can mock for now)

---

## Risks

- Seed data expansion to 125 products is tedious — consider generating with a loop instead of hand-typing
- ConfigPage auto-login flow — need to test carefully with expired tokens
- Role-based filtering — breaking change for Sale users in existing UI; coordinate with Dev B if any field reads importPrice
