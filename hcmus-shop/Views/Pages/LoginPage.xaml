<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="hcmus_shop.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.Background>
                <ImageBrush ImageSource="ms-appx:///Assets/SplashScreen.scale-200.png" Stretch="UniformToFill" />
            </Grid.Background>
            <TextBlock
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontSize="72"
                FontWeight="SemiBold"
                Foreground="White"
                Text="Super Shop" />
        </Grid>

        <Grid Grid.Column="1" Background="{StaticResource LoginRightPanelBrush}">
            <Border
                Width="420"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Style="{StaticResource LoginCardBorderStyle}">
                <StackPanel Spacing="12">
                    <TextBlock FontSize="44" FontWeight="SemiBold" Text="Login" />

                    <TextBlock Margin="0,8,0,0" Text="Username" />
                    <TextBox
                        Style="{StaticResource LoginTextBoxStyle}"
                        Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBlock Margin="0,8,0,0" Text="Password" />
                    <PasswordBox
                        x:Name="PasswordInput"
                        PasswordChanged="PasswordInput_PasswordChanged"
                        Style="{StaticResource LoginPasswordBoxStyle}" />

                    <CheckBox Content="Remember me" IsChecked="{Binding RememberMe, Mode=TwoWay}" />

                    <TextBlock
                        Foreground="IndianRed"
                        Text="{Binding ErrorMessage}"
                        Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}" />

                    <StackPanel
                        HorizontalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="10">
                        <Button
                            Command="{Binding LoginCommand}"
                            Content="Login"
                            Style="{StaticResource LoginPrimaryButtonStyle}" />
                        <Button
                            Command="{Binding UpdateCommand}"
                            Content="Update"
                            Style="{StaticResource LoginOutlineButtonStyle}" />
                    </StackPanel>

                    <Button
                        Margin="0,24,0,0"
                        Command="{Binding OpenConfigCommand}"
                        Content="Config"
                        Style="{StaticResource LoginHyperlinkButtonStyle}" />
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
