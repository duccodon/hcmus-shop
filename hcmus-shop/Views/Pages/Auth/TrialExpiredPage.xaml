<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="hcmus_shop.Views.TrialExpiredPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource LoginRightPanelBrush}">

    <Grid>
        <Border Width="420" HorizontalAlignment="Center" VerticalAlignment="Center"
                Style="{StaticResource LoginCardBorderStyle}">
            <StackPanel Spacing="12">
                <FontIcon HorizontalAlignment="Center" FontSize="40"
                          Foreground="{StaticResource LoginPrimaryBrush}"
                          Glyph="&#xE7BA;" />
                <TextBlock HorizontalAlignment="Center" FontSize="22" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimary}"
                           Text="Trial Expired" />
                <TextBlock HorizontalAlignment="Center" FontSize="13" TextWrapping="Wrap"
                           Foreground="{StaticResource TextSecondary}"
                           Text="Your 15-day trial has ended. Enter your activation code below to continue using HCMUS Shop." />

                <TextBlock Margin="0,12,0,4" FontSize="13" FontWeight="Medium"
                           Foreground="{StaticResource TextPrimary}"
                           Text="Activation Code" />
                <TextBox Style="{StaticResource LoginTextBoxStyle}"
                         PlaceholderText="HCMUS-XXXX-XXXX"
                         Text="{Binding ActivationCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <InfoBar Margin="0,8,0,0"
                         IsOpen="{Binding ErrorMessage, Converter={StaticResource StringToBooleanConverter}}"
                         IsClosable="False"
                         Severity="Error"
                         Message="{Binding ErrorMessage}" />

                <Button Margin="0,12,0,0"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Command="{Binding ActivateCommand}"
                        Content="Activate"
                        FontWeight="SemiBold"
                        Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
