<Page
    x:Class="hcmus_shop.Views.AddProductPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:hcmus_shop.ViewModels"
    xmlns:productVm="using:hcmus_shop.ViewModels.Products"
    x:Name="Root">
    <Grid Background="{StaticResource DashboardBackground}">
        <ScrollViewer>
            <Grid Margin="24" RowSpacing="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button
                        Style="{StaticResource GhostButtonStyle}"
                        Click="BackButton_Click">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE72B;" />
                            <TextBlock Text="Return" />
                        </StackPanel>
                    </Button>
                    <StackPanel
                        Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="8">
                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xEC8A;" FontSize="18" Foreground="{StaticResource TextPrimary}" />
                        <TextBlock
                            Text="Add New Product"
                            Style="{StaticResource CardTitleStyle}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="10">
                        <Button Style="{StaticResource PillButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE70B;" />
                                <TextBlock Text="Save Draft" Style="{StaticResource FieldLabelStyle}" />
                            </StackPanel>
                        </Button>
                        <Button
                            Style="{StaticResource PrimaryPillButtonStyle}"
                            Command="{x:Bind ViewModel.SaveProductCommand}"
                            IsEnabled="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource InverseBooleanConverter}}"
                            Background="{StaticResource LoginPrimaryBrush}"
                            Foreground="White">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE73E;" />
                                <TextBlock Text="Add Product" Style="{StaticResource FieldLabelStyle}" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
                <Grid Grid.Row="1" ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.65*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    <InfoBar
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Margin="0,0,0,10"
                        IsOpen="{x:Bind ViewModel.HasSaveError, Mode=OneWay}"
                        IsClosable="False"
                        Severity="Error"
                        Message="{x:Bind ViewModel.SaveErrorMessage, Mode=OneWay}" />
                    <TextBlock
                        Grid.Column="0"
                        Grid.ColumnSpan="2"
                        Margin="0,22,0,10"
                        Text="{x:Bind ViewModel.SaveStatusMessage, Mode=OneWay}"
                        Foreground="{StaticResource TextSecondary}"
                        Visibility="{x:Bind ViewModel.HasSaveStatus, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <Grid RowSpacing="16">
                        <Grid.Margin>0,26,0,0</Grid.Margin>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Border
                            Background="{StaticResource CardBackground}"
                            CornerRadius="16"
                            Padding="16">
                            <Grid RowSpacing="12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="General Information" Style="{StaticResource CardTitleStyle}" />
                                <Grid Grid.Row="1" RowSpacing="6">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="SKU" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="1" Style="{StaticResource FormTextBoxStyle}" Text="{x:Bind ViewModel.DraftProduct.Sku, Mode=TwoWay}" PlaceholderText="SKU" />
                                </Grid>
                                <Grid Grid.Row="2" RowSpacing="6">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Name Product" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox Grid.Row="1" Style="{StaticResource FormTextBoxStyle}" Text="{x:Bind ViewModel.DraftProduct.Name, Mode=TwoWay}" />
                                </Grid>
                                <Grid Grid.Row="3" RowSpacing="6">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Description Product" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox
                                        Grid.Row="1"
                                        Style="{StaticResource FormTextBoxStyle}"
                                        Text="{x:Bind ViewModel.DraftProduct.Description, Mode=TwoWay}"
                                        TextWrapping="Wrap"
                                        AcceptsReturn="True"
                                        MinHeight="110" />
                                </Grid>
                                <Grid Grid.Row="4" ColumnSpacing="12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid RowSpacing="6">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="Brand" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox
                                            Grid.Row="1"
                                            PlaceholderText="Select brand"
                                            Style="{StaticResource FormComboBoxStyle}"
                                            ItemsSource="{x:Bind ViewModel.BrandOptions, Mode=OneWay}"
                                            DisplayMemberPath="Name"
                                            SelectedValuePath="Id"
                                            SelectedValue="{x:Bind ViewModel.SelectedBrandId, Mode=TwoWay}" />
                                    </Grid>
                                    <Grid Grid.Column="1" RowSpacing="6">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="Series (Optional)" Style="{StaticResource FieldLabelStyle}" />
                                        <ComboBox
                                            Grid.Row="1"
                                            PlaceholderText="Select series"
                                            Style="{StaticResource FormComboBoxStyle}"
                                            ItemsSource="{x:Bind ViewModel.SeriesOptions, Mode=OneWay}"
                                            DisplayMemberPath="Name"
                                            SelectedValuePath="Id"
                                            SelectedValue="{x:Bind ViewModel.SelectedSeriesId, Mode=TwoWay}" />
                                    </Grid>
                                </Grid>
                                <Grid Grid.Row="5" RowSpacing="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Specifications (JSONB)" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBox
                                        Grid.Row="1"
                                        Style="{StaticResource FormTextBoxStyle}"
                                        Text="{x:Bind ViewModel.DraftProduct.Specifications, Mode=TwoWay}"
                                        TextWrapping="Wrap"
                                        AcceptsReturn="True"
                                        MinHeight="120"
                                        PlaceholderText="Enter JSON specifications" />
                                    <TextBlock Grid.Row="2" Text="Warranty Months" Style="{StaticResource FieldLabelStyle}" />
                                    <NumberBox
                                        Grid.Row="3"
                                        Style="{StaticResource FormNumberBoxStyle}"
                                        Value="{x:Bind ViewModel.WarrantyMonthsValue, Mode=TwoWay}"
                                        SpinButtonPlacementMode="Inline"
                                        Minimum="0" />
                                </Grid>
                            </Grid>
                        </Border>
                        <Border
                            Grid.Row="1"
                            Background="{StaticResource CardBackground}"
                            CornerRadius="16"
                            Padding="16">
                            <Grid RowSpacing="12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Pricing And Stock" Style="{StaticResource CardTitleStyle}" />
                                <Grid Grid.Row="1" ColumnSpacing="12" RowSpacing="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Import Price" Style="{StaticResource FieldLabelStyle}" />
                                    <TextBlock Grid.Column="1" Text="Selling Price" Style="{StaticResource FieldLabelStyle}" />
                                    <NumberBox
                                        Grid.Row="1"
                                        Style="{StaticResource FormNumberBoxStyle}"
                                        Value="{x:Bind ViewModel.ImportPriceValue, Mode=TwoWay}"
                                        SpinButtonPlacementMode="Inline"
                                        Minimum="0" />
                                    <NumberBox
                                        Grid.Row="1"
                                        Grid.Column="1"
                                        Style="{StaticResource FormNumberBoxStyle}"
                                        Value="{x:Bind ViewModel.SellingPriceValue, Mode=TwoWay}"
                                        SpinButtonPlacementMode="Inline"
                                        Minimum="0" />

                                    <TextBlock
                                        Grid.Row="2"
                                        Text="Stock Quantity"
                                        FontWeight="SemiBold"
                                        Foreground="{StaticResource TextPrimary}" />

                                    <NumberBox
                                        Grid.Row="3"
                                        Value="{x:Bind ViewModel.StockQuantityValue, Mode=TwoWay}"
                                        SpinButtonPlacementMode="Inline"
                                        Minimum="0" />
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                    <Grid Grid.Column="1" RowSpacing="16">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Border
                            Background="{StaticResource CardBackground}"
                            CornerRadius="16"
                            Padding="16">
                            <Grid RowSpacing="12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Upload Img" Style="{StaticResource CardTitleStyle}" />
                                <Border
                                    Grid.Row="1"
                                    CornerRadius="12"
                                    Background="#FFEAF4E8"
                                    BorderBrush="#22000000"
                                    BorderThickness="1"
                                    MinHeight="290"
                                    Padding="8">
                                    <Grid>
                                        <Image
                                            Source="{x:Bind ViewModel.MainPreview.Bitmap, Mode=OneWay}"
                                            Stretch="UniformToFill"
                                            Visibility="{x:Bind ViewModel.HasMainPreview, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                        <StackPanel
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Spacing="10"
                                            Visibility="{x:Bind ViewModel.HasMainPreview, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                                            <Border
                                                Width="110"
                                                Height="110"
                                                CornerRadius="55"
                                                Background="{StaticResource ImageUploadPlaceholderBackground}">
                                                <FontIcon
                                                    FontFamily="Segoe Fluent Icons"
                                                    Glyph="&#xEC8A;"
                                                    FontSize="48"
                                                    Foreground="{StaticResource LoginPrimaryBrush}"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                            </Border>
                                            <TextBlock Text="Main image preview" Foreground="{StaticResource TextSecondary}" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                                <Grid Grid.Row="2" ColumnSpacing="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Button
                                        Width="72"
                                        Height="72"
                                        Style="{StaticResource GhostButtonStyle}"
                                        Click="AddImageButton_Click"
                                        Background="{StaticResource ImageUploadButtonBackground}"
                                        Foreground="{StaticResource LoginPrimaryBrush}">
                                        <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE710;" FontSize="18" />
                                    </Button>
                                    <ScrollViewer
                                        Grid.Column="1"
                                        HorizontalScrollBarVisibility="Auto"
                                        VerticalScrollBarVisibility="Disabled"
                                        HorizontalScrollMode="Enabled"
                                        VerticalScrollMode="Disabled"
                                        ZoomMode="Disabled">
                                        <ItemsControl ItemsSource="{x:Bind ViewModel.PreviewImages, Mode=OneWay}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Spacing="8" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="vm:ImagePreview">
                                                    <Button
                                                        Width="72"
                                                        Height="72"
                                                        Padding="0"
                                                        Background="{StaticResource ImageUploadPreviewBackground}"
                                                        BorderBrush="{StaticResource TableRowBorder}"
                                                        BorderThickness="1"
                                                        CornerRadius="8"
                                                        Command="{Binding DataContext.SelectPreviewCommand, ElementName=Root}"
                                                        CommandParameter="{x:Bind}">
                                                        <Image Source="{x:Bind Bitmap, Mode=OneWay}" Stretch="UniformToFill" />
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </Grid>
                        </Border>
                        <Border
                            Grid.Row="1"
                            Background="{StaticResource CardBackground}"
                            CornerRadius="16"
                            Padding="16">
                            <Grid RowSpacing="12">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="Category" Style="{StaticResource CardTitleStyle}" />
                                <TextBlock Grid.Row="1" Text="Product Category (Multiple)" Style="{StaticResource FieldLabelStyle}" />
                                <ItemsControl Grid.Row="2" ItemsSource="{x:Bind ViewModel.CategoryOptions, Mode=OneWay}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="productVm:CategoryOptionViewModel">
                                            <CheckBox
                                                Margin="0,0,0,6"
                                                Content="{x:Bind Name, Mode=OneWay}"
                                                IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Button
                                    Grid.Row="3"
                                    Margin="0,6,0,0"
                                    Content="{x:Bind ViewModel.AddCategoryButtonText, Mode=OneWay}"
                                    Style="{StaticResource PrimaryPillButtonStyle}"
                                    HorizontalAlignment="Left"
                                    Command="{x:Bind ViewModel.AddCategoryCommand}"
                                    Background="{StaticResource LoginPrimaryBrush}"
                                    Foreground="White" />

                                <TextBlock
                                    Grid.Row="3"
                                    Margin="150,12,0,0"
                                    VerticalAlignment="Center"
                                    Foreground="{StaticResource TextSecondary}"
                                    Text="Working..."
                                    Visibility="{x:Bind ViewModel.IsAddingCategory, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                <TextBlock
                                    Grid.Row="3"
                                    Margin="0,44,0,0"
                                    Foreground="{StaticResource DangerForeground}"
                                    Text="{x:Bind ViewModel.CategoryErrorMessage, Mode=OneWay}"
                                    Visibility="{x:Bind ViewModel.HasCategoryError, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
