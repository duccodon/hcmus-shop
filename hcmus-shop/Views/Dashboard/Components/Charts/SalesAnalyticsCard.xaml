<UserControl
    x:Class="hcmus_shop.Views.Dashboard.Components.Charts.SalesAnalyticsCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    xmlns:local="using:hcmus_shop.Views.Dashboard.Components.Controls">

    <Border
        Background="{StaticResource CardBackground}"
        CornerRadius="16"
        Padding="16">
        <StackPanel Spacing="12">
            <TextBlock Text="Sales Analytics" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" FontSize="18" />
            <lvc:CartesianChart
                Height="250"
                Series="{x:Bind Series, Mode=OneWay}"
                XAxes="{x:Bind XAxes, Mode=OneWay}"
                YAxes="{x:Bind YAxes, Mode=OneWay}"
                LegendPosition="Hidden" />
        </StackPanel>
    </Border>
</UserControl>
